/* =====================================================
   CALENDAR DAY
===================================================== */

export interface CalendarDay
{
    /* ===================================================
       DATE
    ==================================================== */

    date: Date;

    /* ===================================================
       DAY NUMBER
    ==================================================== */

    day: number;

    /* ===================================================
       MONTH FLAGS
    ==================================================== */

    isCurrentMonth: boolean;

    isPreviousMonth: boolean;

    isNextMonth: boolean;

    /* ===================================================
       STATUS
    ==================================================== */

    isToday: boolean;

    isSelected: boolean;

    isDisabled: boolean;

    /* ===================================================
       WEEKEND
    ==================================================== */

    isWeekend: boolean;
}